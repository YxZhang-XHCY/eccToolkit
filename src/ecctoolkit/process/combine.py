"""Combine FLED and CircleMap results."""

import logging

logger = logging.getLogger(__name__)


def combine_fled_circlemap(
    fled_dir: str,
    circlemap_dir: str,
    output_dir: str,
) -> None:
    """Combine FLED and CircleMap results."""
    logger.info("Combining FLED and CircleMap results - placeholder")
    raise NotImplementedError("Pending migration from combine_fled_circlemap.py")
